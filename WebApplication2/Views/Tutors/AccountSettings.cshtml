@model  WebApplication2.Models.Payment
@{
    ViewBag.Title = "Account";
}

<div class="container fill img-rounded">

    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10 personal-info">
            <h1 class="h1over">Account Settings</h1>
            <hr>
        </div>
    </div>
  
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-2" style="border:dotted">
            <br/>
            <center>
                <h1>@Model.Amount$</h1>
                <h6>Current Balance</h6>
            </center>
        </div>
        <div class="col-md-1"></div>
        <div class="col-md-8 personal-info">
           

            <h3> Add Funds to your Account</h3><br/><br/>
              @using (Html.BeginForm("Payment", "Tutors", FormMethod.Post, new { id = "QuestionDetails" }))
               {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <label class="col-lg-3 control-label">@Html.Label("Amount:")</label>
                        <div class="col-lg-6">
                            @Html.EditorFor(model => model.Amount, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.Amount, "", new { @class = "text-danger" })
                        </div>

                        <div class="form-group">
                            <label class="col-md-3 control-label"></label>
                            <div class="col-md-4"></div>
                                <div class="col-md-6">
                                    <br/>
                                    <input type="submit" class="btn btn-success" value="Add Funds">
                                    
                                </div>
                            </div>
                    </div>

                }
        </div>

   </div>
</div>


@section scripts{
   <script>

      
    </script>
   
    
    }
